{
  "name": "file-scan-sandbox",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "sudo apt update && sudo apt install -y clamav clamav-daemon unzip p7zip-full unrar-free megatools && sudo freshclam"
  "customizations": {
    "vscode": {
      "extensions": []
    }
  }
}
